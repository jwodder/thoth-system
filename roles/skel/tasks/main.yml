---
- name: Store jwodder_root var for later roles
  ansible.builtin.set_fact:
    # This makes the variable accessible to subsequent roles, right?
    jwodder_root: "{{ jwodder_root }}"  # noqa: var-naming[no-role-prefix]

- name: Create jwodder package skeleton
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    owner: "{{ admin_user }}"
    mode: "0755"
  with_items:
    - "{{ jwodder_root }}/bin"
    - "{{ jwodder_root }}/etc"
    - "{{ jwodder_root }}/share"
    - "{{ jwodder_root }}/venvs"

- name: Create secrets directory
  ansible.builtin.file:
    path: "{{ jwodder_root }}/etc/secret"
    state: directory
    owner: root
    group: root
    mode: "0700"

- name: Create logs directory
  ansible.builtin.file:
    path: "{{ jwodder_root }}/logs"
    state: directory
    owner: "{{ admin_user }}"
    group: syslog
    mode: "0775"

- name: Keep venvs directory from being backed up
  ensure_file:
    path: "{{ jwodder_root }}/venvs/.norsync"
    owner: "{{ admin_user }}"
    mode: "0644"
