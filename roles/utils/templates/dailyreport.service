[Unit]
Description=Send daily report e-mail

[Service]
Type=oneshot
ExecStart=/home/{{ admin_user }}/.local/bin/dailyreport {{ jwodder_root }}/etc/utils.toml
#ExecStopPost=/bin/sh -c 'if [ "$$SERVICE_RESULT" != success ]; then /usr/local/bin/mail-systemd-failure %n; fi'
User={{ admin_user }}
Group={{ admin_user }}
